#! /usr/bin/python3

import numpy as np

linecomment = '='*80

print( np.identity(3) )
print( linecomment )
print( np.ones(3) )

# zeros( (n,m) )
# ones( (n,m) )
# diag(v,k)
# random.rand(n,m)
# arange(n)
# linspace(a,b,n)